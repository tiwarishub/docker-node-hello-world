apiVersion: v1
kind: Service
metadata:
    name: "aatiwarishubaks-d08e"
    labels:
        app: "aatiwarishubaks-d08e"
spec:
    type: ClusterIP
    ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
    selector:
        app: "aatiwarishubaks-d08e"
