apiVersion: v1
kind: Service
metadata:
    name: "aatiwarishubaks-139b"
    labels:
        app: "aatiwarishubaks-139b"
spec:
    type: LoadBalancer
    ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
    selector:
        app: "aatiwarishubaks-139b"