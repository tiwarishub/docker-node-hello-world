apiVersion: v1
kind: Service
metadata:
    name: "aatiwarishubaks-d8f1"
    labels:
        app: "aatiwarishubaks-d8f1"
spec:
    type: LoadBalancer
    ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
    selector:
        app: "aatiwarishubaks-d8f1"