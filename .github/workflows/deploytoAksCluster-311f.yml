on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: aaaatiwarishubregistry.azurecr.io
        username: ${{secrets.acr_aaaatiwarishubregistry_username}}
        password: ${{secrets.acr_aaaatiwarishubregistry_password}}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/." -f  "Dockerfile" -t aaaatiwarishubregistry.azurecr.io/aatiwarishub2aks311f:${{ github.sha }}
        docker push aaaatiwarishubregistry.azurecr.io/aatiwarishub2aks311f:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{secrets.aks_aatiwarishub2aks_kubeConfig}}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep aatiwarishub2aks311f | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace aatiwarishub2aks311f`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: aatiwarishub2aks311f
        container-registry-url: aaaatiwarishubregistry.azurecr.io
        container-registry-username: ${{secrets.acr_aaaatiwarishubregistry_username}}
        container-registry-password: ${{secrets.acr_aaaatiwarishubregistry_password}}
        secret-name: aatiwarishubdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: aatiwarishub2aks311f
        manifests: |
          ./manifests/deployment-311f.yml
          ./manifests/service-311f.yml
        images: |
          aaaatiwarishubregistry.azurecr.io/aatiwarishub2aks311f:${{ github.sha }}
        imagepullsecrets: |
          aatiwarishubdockerauth