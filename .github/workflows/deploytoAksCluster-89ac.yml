on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{secrets.aatiwarishub5aksacr_89ac}}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n aatiwarishub5aksacr -g aatiwarishub5aksacra234-rg --location "South Central US" --sku Basic --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n aatiwarishub5aksacr --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n aatiwarishub5aksacr --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n aatiwarishub5aksacr --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: aatiwarishub5aksacr.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/." -f  "Dockerfile" -t aatiwarishub5aksacr.azurecr.io/aatiwarishubaks89ac:${{ github.sha }}
        docker push aatiwarishub5aksacr.azurecr.io/aatiwarishubaks89ac:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{secrets.aks_aatiwarishubaks_kubeConfig}}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep aatiwarishubaks89ac | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace aatiwarishubaks89ac`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: aatiwarishubaks89ac
        container-registry-url: aatiwarishub5aksacr.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: aatiwarishubdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: aatiwarishubaks89ac
        manifests: |
          ./manifests/deployment-89ac.yml
          ./manifests/service-89ac.yml
        images: |
          aatiwarishub5aksacr.azurecr.io/aatiwarishubaks89ac:${{ github.sha }}
        imagepullsecrets: |
          aatiwarishubdockerauth